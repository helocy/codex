# 数据库配置
POSTGRES_SERVER=localhost
POSTGRES_USER=memory
POSTGRES_PASSWORD=memory123
POSTGRES_DB=memory_db
POSTGRES_PORT=5432

# 文件存储
UPLOAD_DIR=./uploads

# 本地 Embedding 模型（免费，无需配置）
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
EMBEDDING_DIM=384

# 豆包大模型 API（可选）
DOUBAO_API_KEY=
DOUBAO_MODEL=doubao-seed-1-6-251015

# 网络搜索配置（可选）
# WEB_SEARCH_PROVIDER=duckduckgo  # 免费，无需 API Key
# SERPER_API_KEY=                   # 可选，需要 https://serper.dev API Key
